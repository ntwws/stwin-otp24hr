{
  "name": "hero-line-coordinator",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute hero-line-data --local --file=schema.sql",
    "db:remote": "wrangler d1 execute hero-line-data --remote --file=schema.sql"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
